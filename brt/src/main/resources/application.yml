spring:
  application:
    name: brt-service
  rabbitmq:
    host: localhost
    port: 5671
    username: admin
    password: password

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
